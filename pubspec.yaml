name: ai
description: A AI terminal helper for developers.
version: 0.2.0

environment:
  sdk: ^3.7.2

# Add regular dependencies here.
dependencies:
  args: ^2.7.0
  http: ^1.4.0
  path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
